module.exports = {
    plugins: [
        require('autoprefixer'),     // Autoprefixer plugin
        require('tailwindcss'),      // Tailwind CSS plugin
        require('postcss-rtlcss'),    // PostCSS RTL plugin
    ],
};
